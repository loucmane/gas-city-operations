# ga-e0t1.13 — reviewed context recovery source

## Scope

This source repair belongs to the existing ga-e0t1 workflow. It prepares the
explicitly authorized append-forward recovery of ga-tmgr; neither Bead nor the
unfinished parent is complete merely because the source is delivered.

- Inspect the selected immutable base for inherited unrelated ACTIVE context
  before `begin` creates a journal, branch or worktree, including dry-run.
- Offer explicit digest-bound `reconcile-context` preview/apply. Preserve original
  journals and both worktrees; retire only the unowned partial child context and
  use the existing dependency/attachment/ownership APIs for one parent binding.
- Require exact before/after facts and immediate no-op replay. Refuse conflicting
  ownership, unsafe inputs, unexpected changes and uncertain partial coordination.
- Add no permission exemption, routing, worker, cache repair or root capability.

## Retained verification

- Focused source regression: 164 passed.
- Final adapter/meta-workflow suites: 3092 passed, four existing opt-in skips,
  zero failures/errors. JUnit SHA256:
  `aeefdff2419530921d7c761e5c31f90c4c2c8cee3e4bd6148466f97921587b84`.
- Generated consumer-plan parity, task guard, strict drift and diff checks passed.
- Fresh independent Fable R3: conditional SOURCE PASS, no must-fix defects.
  Result SHA256:
  `3e5fe214a017007bb0dc0fce6df87a861efe4e73977d9d9619b3de770baa1862`.
  This was subscription-only, tool-free source review, not reviewer-run tests.
- Independent installed-API evidence confirms empty reverse results are arrays,
  reverse edges are immediate rather than transitive, and row IDs identify the
  dependent. The existing two-hop blocks chain was read without mutation.
  Evidence SHA256:
  `64472a13a183f78fba82866c7eef7944e0c637cb85b87d28b6f9030abda4a5a1`.

The candidate source digests still match the reviewed and tested files. R3 and
the completed API checks are reused, not repeated. R1's source verdict does not
stand in for the corrected candidate; R2's consumed authentication failure and
all preparation failures remain preserved separately.

## Remaining gates

Require clean signed delivery, exact-head/current-base required CI, clean
mergeability and no unresolved review threads before merge. Then bind activation
to the exact merged source, inspect the fresh recovery preview and review the
exact application package before applying reconciliation. Preserve original
snapshots and prove one owner, permitted deltas and immediate no-op afterward.

Live facts are non-atomic: one executing coordinator is still required. This
source does not prove Core metadata-only adoption, enforced cache write denial,
Claude managed signing or either provider handover direction. Those separate
acceptance obligations and all existing stop conditions remain in force.
